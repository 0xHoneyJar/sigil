/**
 * Sigil v1.2.4 - Glass Recipe Set
 *
 * For marketing pages, landing pages, and showcase interfaces.
 * Maximum delight - float, glow, staggered entrances.
 *
 * Physics profile:
 * - Feel: Premium, elegant, delightful
 * - Sync: client_authoritative
 * - Base spring: (200, 20) with hover effects
 *
 * @example
 * ```tsx
 * import { HeroCard, FeatureCard, Tooltip } from '@sigil/recipes/glass';
 * ```
 */

// Hero components
export { HeroCard, type HeroCardProps } from './hero-card';

// Feature displays
export { FeatureCard, type FeatureCardProps } from './feature-card';

// Contextual UI
export { Tooltip, type TooltipProps } from './tooltip';
