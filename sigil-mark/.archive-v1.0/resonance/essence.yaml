# Sigil Resonance: Essence
# Product soul captured through /envision interview.
# This is a TEMPLATE — values populated by running /envision

essence:
  # ═══════════════════════════════════════════════════════════════════
  # PRODUCT IDENTITY
  # ═══════════════════════════════════════════════════════════════════

  identity:
    name: ""                    # Product name
    tagline: ""                 # One-line description
    version: ""                 # Current version (e.g., "1.0.0")
    captured_date: ""           # When /envision was run

  # ═══════════════════════════════════════════════════════════════════
  # SOUL STATEMENT
  # ═══════════════════════════════════════════════════════════════════

  soul:
    statement: |
      # Fill in during /envision
      # Example: "This product feels like a trusted craftsman's tool—
      # reliable, precise, with a subtle warmth that rewards expertise."

    invariants:
      # Things that must ALWAYS be true, no matter what
      # Example:
      # - "Critical actions always require explicit confirmation"
      # - "Speed never comes at the cost of clarity"
      # - "Every interaction has a clear outcome"
      []

    anti_invariants:
      # Things that must NEVER be true
      # Example:
      # - "Never feel corporate or impersonal"
      # - "Never prioritize flash over function"
      # - "Never hide critical information behind interaction"
      []

  # ═══════════════════════════════════════════════════════════════════
  # REFERENCE PRODUCTS
  # ═══════════════════════════════════════════════════════════════════

  references:
    games:
      # Games that capture the desired feel
      # Format: { name: "", why: "" }
      []

    apps:
      # Apps that capture the desired feel
      # Format: { name: "", why: "" }
      []

    physical:
      # Physical products that capture the desired feel
      # Format: { name: "", why: "" }
      []

  # ═══════════════════════════════════════════════════════════════════
  # FEEL DESCRIPTORS
  # ═══════════════════════════════════════════════════════════════════

  feel:
    at_first_glance:
      # How should it feel on first impression?
      # Example: "Clean, professional, quietly confident"
      ""

    during_use:
      # How should it feel while using?
      # Example: "Responsive, predictable, pleasantly efficient"
      ""

    after_completion:
      # How should it feel after completing a task?
      # Example: "Satisfied, accomplished, trusted"
      ""

    on_error:
      # How should it feel when something goes wrong?
      # Example: "Clear about what happened, confident it can be fixed"
      ""

  # ═══════════════════════════════════════════════════════════════════
  # ANTI-PATTERNS
  # ═══════════════════════════════════════════════════════════════════

  anti_patterns:
    # Things this product should NEVER feel like
    # Example:
    # - pattern: "Corporate enterprise software"
    #   why: "Should feel approachable, not bureaucratic"
    # - pattern: "Gamified productivity app"
    #   why: "Rewards should feel meaningful, not manipulative"
    []

  # ═══════════════════════════════════════════════════════════════════
  # KEY MOMENTS
  # ═══════════════════════════════════════════════════════════════════

  key_moments:
    high_stakes:
      # Moments where the user is anxious, careful, or invested
      # Example:
      # - moment: "Confirming a large transaction"
      #   feel: "Deliberate, trustworthy, no ambiguity"
      []

    celebration:
      # Moments where the user should feel rewarded
      # Example:
      # - moment: "Successfully claiming a reward"
      #   feel: "Triumphant, satisfying, earned"
      []

    recovery:
      # Moments where the user is recovering from error
      # Example:
      # - moment: "Transaction failed, needs retry"
      #   feel: "Clear path forward, not blamed, supported"
      []

    discovery:
      # Moments where the user is exploring
      # Example:
      # - moment: "Browsing available options"
      #   feel: "Curious, delighted by details, no pressure"
      []

  # ═══════════════════════════════════════════════════════════════════
  # TENSIONS (Product-Level Defaults)
  # ═══════════════════════════════════════════════════════════════════

  tensions:
    # Product-wide tension defaults (can be overridden by zone)
    playfulness: 50   # 0=serious, 100=fun
    weight: 50        # 0=light, 100=heavy
    density: 50       # 0=spacious, 100=dense
    speed: 50         # 0=slow, 100=fast

# ═══════════════════════════════════════════════════════════════════
# INSTRUCTIONS
# ═══════════════════════════════════════════════════════════════════
#
# This file is populated by running /envision.
# The interview will ask about:
#   1. Product identity and purpose
#   2. Reference products (games, apps, physical objects)
#   3. How it should feel at different moments
#   4. What it should never feel like
#   5. Overall tension settings
#
# After /envision completes, this file becomes the soul of your product.
# All design decisions should resonate with these values.
